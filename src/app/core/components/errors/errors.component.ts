import { Component } from '@angular/core';

/**
 * Used for showing errors from `<mat-error>` but limiting to a single one.
 * This also includes the check whether errors are set.
 */
@Component({
  selector: 'app-errors',
  templateUrl: './errors.component.html',
  styleUrls: ['./errors.component.scss'],
})
export class ErrorsComponent {
}
